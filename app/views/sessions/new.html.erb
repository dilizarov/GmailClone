new to Gmail? <%= button_to "Create an account", new_user_url, { :method => :get } %>

<br><br><h1>Sign In</h1>

<%= flash.now[:errors][0] %>

<form action="<%= session_url %>" method="post">
  <input
     name="authenticity_token"
     type="hidden"
     value="<%= form_authenticity_token %>">

  <label for="user_username">Email</label>
  <input
     id="user_email"
     name="user[email]"
     type="text"
     value="<%= @user.email %>">
  <br>

  <label for="user_password">Password</label>
  <input
     id="user_password"
     name="user[password]"
     type="password">
  <br>

  <input type="submit" value="Log in">
</form>